name: V15 stabilization patch

on:
  push:
    branches:
      - feat/v15-ux-navigation-calls
    paths:
      - .github/workflows/v15-stabilize-patch.yml

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/v15-ux-navigation-calls
      - name: Patch exact V15 audit findings
        run: |
          python - <<'PY'
          from pathlib import Path

          calls = Path('app/(tabs)/calls.tsx')
          text = calls.read_text()
          old = 'newAppointment: { minHeight: 38,'
          if text.count(old) != 1:
              raise SystemExit(f'Expected one Calls target, got {text.count(old)}')
          calls.write_text(text.replace(old, 'newAppointment: { minHeight: 48,', 1))

          audit = Path('scripts/visual-audit.cjs')
          text = audit.read_text()
          old_case = '{ name: "highlights-map-390x844", width: 390, height: 844, route: "/highlights", clickLabel: "Afficher la carte" },'
          new_case = '{ name: "highlights-map-390x844", width: 390, height: 844, route: "/highlights", clickText: "Map" },'
          if text.count(old_case) != 1:
              raise SystemExit(f'Expected one Map case, got {text.count(old_case)}')
          text = text.replace(old_case, new_case, 1)

          old_runner = '''      if (testCase.clickLabel) {\n        await page.getByLabel(testCase.clickLabel).click();\n        await page.waitForTimeout(350);\n      }\n'''
          new_runner = '''      if (testCase.clickLabel) {\n        await page.getByLabel(testCase.clickLabel).click();\n        await page.waitForTimeout(350);\n      }\n      if (testCase.clickText) {\n        await page.getByText(testCase.clickText, { exact: true }).click();\n        await page.waitForTimeout(350);\n      }\n'''
          if text.count(old_runner) != 1:
              raise SystemExit(f'Expected one click runner, got {text.count(old_runner)}')
          audit.write_text(text.replace(old_runner, new_runner, 1))
          PY
      - name: Commit exact patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add 'app/(tabs)/calls.tsx' scripts/visual-audit.cjs
          git commit -m "fix: stabilize V15 browser audits"
          git push origin HEAD:feat/v15-ux-navigation-calls
