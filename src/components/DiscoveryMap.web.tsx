import { createElement, useEffect, useMemo, useRef } from "react";
import { StyleSheet, View } from "react-native";

import { getDiscoveryEventProximity } from "../domain/discoveryEvents";
import { getRoleAppearance } from "../domain/roleAppearance";
import { useAppTheme } from "../providers/ThemeProvider";
import type { DiscoveryMapProps } from "./DiscoveryMap.types";

function escapeInline(value: unknown): string {
  return JSON.stringify(value).replaceAll("<", "\\u003c");
}

export default function DiscoveryMap({ moments, events, selectedEntity, onSelectEntity }: DiscoveryMapProps) {
  const theme = useAppTheme();
  const iframeRef = useRef<HTMLIFrameElement | null>(null);

  useEffect(() => {
    const listener = (event: MessageEvent) => {
      if (event.data?.source !== "connexio-discovery-map" || event.data?.type !== "entity-selected") return;
      if ((event.data.kind !== "person" && event.data.kind !== "event") || typeof event.data.id !== "string") return;
      onSelectEntity({ kind: event.data.kind, id: event.data.id });
    };
    window.addEventListener("message", listener);
    return () => window.removeEventListener("message", listener);
  }, [onSelectEntity]);

  useEffect(() => {
    iframeRef.current?.contentWindow?.postMessage({ source: "connexio-discovery-parent", type: "selection", selection: selectedEntity ?? null }, "*");
  }, [selectedEntity]);

  const html = useMemo(() => {
    const people = moments.map((moment) => {
      const role = getRoleAppearance(moment.member.role);
      return { id: moment.member.id, name: moment.member.name, initials: moment.member.initials, avatarUrl: moment.member.avatarUrl ?? null, roleColor: role.border, latitude: moment.latitude, longitude: moment.longitude, pulse: moment.recentPostIds.length > 0 };
    });
    const eventMarkers = events.map((event) => ({ id: event.id, title: event.title, latitude: event.latitude, longitude: event.longitude, proximity: getDiscoveryEventProximity(event) }));
    const tileStyle = theme.isLight ? "light_all" : "dark_all";
    return `<!doctype html><html lang="fr"><head><meta charset="utf-8"/><meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no"/><link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"/><link rel="stylesheet" href="https://unpkg.com/leaflet.markercluster@1.5.3/dist/MarkerCluster.css"/><link rel="stylesheet" href="https://unpkg.com/leaflet.markercluster@1.5.3/dist/MarkerCluster.Default.css"/><style>
:root{--bg:${theme.pageBackground};--surface:${theme.surface};--text:${theme.pageText};--muted:${theme.pageTextMuted};--border:${theme.border};--shell:${theme.shellBackground}}html,body,#map{height:100%;margin:0;background:var(--bg);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.leaflet-control-attribution{background:rgba(0,0,0,.1)!important;color:var(--muted)!important;font-size:7px!important}.leaflet-control-attribution a{color:var(--muted)!important}.leaflet-tooltip{background:var(--shell)!important;color:var(--text)!important;border:1px solid var(--border)!important;border-radius:10px!important;font-weight:800;box-shadow:none!important}.person-marker{width:62px;height:74px;position:relative;display:flex;align-items:flex-start;justify-content:center}.person-character{position:relative;width:42px;height:67px;filter:drop-shadow(0 5px 8px rgba(10,35,65,.25));transition:transform .2s ease}.person-head{position:absolute;top:0;left:4px;width:34px;height:34px;border-radius:15px;border:3px solid var(--role);background:var(--surface);overflow:hidden;display:grid;place-items:center;color:var(--text);font-size:10px;font-weight:900;z-index:3}.person-head img{width:100%;height:100%;object-fit:cover}.person-body{position:absolute;top:29px;left:7px;width:28px;height:31px;border-radius:14px 14px 10px 10px;background:linear-gradient(160deg,var(--role),#0b4f8f);border:2px solid var(--surface);z-index:2}.person-body:after{content:"";position:absolute;left:5px;right:5px;bottom:-7px;height:12px;border-radius:8px;background:var(--role);transform:skewX(-10deg)}.person-marker.pulse:before{content:"";position:absolute;top:-2px;width:48px;height:48px;border-radius:50%;border:2px solid var(--role);opacity:.35;animation:personPulse 4s ease-out infinite}.person-marker.selected .person-character{transform:translateY(-3px) scale(1.12)}.event-marker{width:52px;height:52px;position:relative;display:grid;place-items:center;--ping:#2686d6}.event-marker.live{--ping:#00cdb8}.event-marker.within48h{--ping:#16b4d3}.event-marker.within7d{--ping:#2b93d1}.event-marker.later{--ping:#316cb2}.event-marker.past24h{--ping:#69aee1}.event-core{width:22px;height:22px;border-radius:50%;background:var(--ping);border:4px solid rgba(255,255,255,.94);box-shadow:0 5px 14px rgba(15,70,120,.25);position:relative;z-index:2;transition:transform .18s ease}.event-core:after{content:"";position:absolute;left:50%;top:17px;width:10px;height:10px;background:var(--ping);transform:translateX(-50%) rotate(45deg);border-radius:2px}.event-marker.live:before,.event-marker.within48h:before,.event-marker.within7d:before,.event-marker.past24h:before{content:"";position:absolute;inset:7px;border-radius:50%;border:2px solid var(--ping);animation:eventPulse 3.4s ease-out infinite}.event-marker.live:before{animation-duration:1.8s}.event-marker.within48h:before{animation-duration:2.4s}.event-marker.within7d:before{animation-duration:3.6s}.event-marker.past24h:before{animation-duration:4.6s;opacity:.5}.event-marker.selected .event-core{transform:scale(1.28)}.custom-cluster{background:transparent!important;border:none!important}.cluster-core{width:42px;height:42px;border-radius:50%;border:3px solid rgba(255,255,255,.92);background:#1a88c9;color:#fff;display:grid;place-items:center;font-weight:900;box-shadow:0 6px 16px rgba(20,60,100,.22)}.geo-btn{position:absolute;right:10px;top:10px;z-index:999;width:46px;height:46px;border:0;border-radius:23px;background:var(--shell);color:var(--text);font-size:20px;cursor:pointer;box-shadow:0 5px 16px rgba(30,50,75,.16)}@keyframes personPulse{0%{opacity:.35;transform:scale(.82)}80%,100%{opacity:0;transform:scale(1.3)}}@keyframes eventPulse{0%{opacity:.62;transform:scale(.72)}78%,100%{opacity:0;transform:scale(1.55)}}</style></head><body><div id="map"></div><button class="geo-btn" aria-label="Me localiser">◎</button><script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script><script src="https://unpkg.com/leaflet.markercluster@1.5.3/dist/leaflet.markercluster.js"></script><script>
const people=${escapeInline(people)};const events=${escapeInline(eventMarkers)};const nodes=new Map();const escapeText=value=>String(value).replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[char]));const map=L.map('map',{zoomControl:false,minZoom:5,maxZoom:17,zoomSnap:.25});L.tileLayer('https://{s}.basemaps.cartocdn.com/${tileStyle}/{z}/{x}/{y}{r}.png',{subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap &copy; CARTO'}).addTo(map);const cluster=L.markerClusterGroup({maxClusterRadius:54,spiderfyOnMaxZoom:true,showCoverageOnHover:false,iconCreateFunction:c=>L.divIcon({className:'custom-cluster',html:'<div class="cluster-core">'+c.getChildCount()+'</div>',iconSize:[42,42]})});const bounds=[];
people.forEach(person=>{const face=person.avatarUrl?'<img src="'+escapeText(person.avatarUrl)+'" alt=""/>':escapeText(person.initials);const html='<div class="person-marker '+(person.pulse?'pulse':'')+'" data-key="person:'+escapeText(person.id)+'" style="--role:'+escapeText(person.roleColor)+'"><div class="person-character"><div class="person-head">'+face+'</div><div class="person-body"></div></div></div>';const marker=L.marker([person.latitude,person.longitude],{icon:L.divIcon({className:'',html,iconSize:[62,74],iconAnchor:[31,64]}),title:person.name});marker.on('add',()=>{const node=marker.getElement()?.querySelector('.person-marker');if(node)nodes.set('person:'+person.id,node)});marker.on('click',()=>window.parent.postMessage({source:'connexio-discovery-map',type:'entity-selected',kind:'person',id:person.id},'*'));marker.bindTooltip(escapeText(person.name),{direction:'bottom',offset:[0,8],opacity:.96});cluster.addLayer(marker);bounds.push([person.latitude,person.longitude]);});
events.forEach(event=>{const html='<div class="event-marker '+escapeText(event.proximity)+'" data-key="event:'+escapeText(event.id)+'"><div class="event-core"></div></div>';const marker=L.marker([event.latitude,event.longitude],{icon:L.divIcon({className:'',html,iconSize:[52,52],iconAnchor:[26,40]}),title:event.title,zIndexOffset:160});marker.on('add',()=>{const node=marker.getElement()?.querySelector('.event-marker');if(node)nodes.set('event:'+event.id,node)});marker.on('click',()=>window.parent.postMessage({source:'connexio-discovery-map',type:'entity-selected',kind:'event',id:event.id},'*'));marker.bindTooltip(escapeText(event.title),{direction:'bottom',offset:[0,10],opacity:.96});cluster.addLayer(marker);bounds.push([event.latitude,event.longitude]);});map.addLayer(cluster);if(bounds.length){map.fitBounds(bounds,{padding:[36,36],maxZoom:9})}else{map.setView([43.45,2.6],7)}function updateSelection(selection){nodes.forEach((node,key)=>node.classList.toggle('selected',Boolean(selection)&&key===selection.kind+':'+selection.id));}window.addEventListener('message',event=>{const data=event.data;if(data?.source==='connexio-discovery-parent'&&data?.type==='selection')updateSelection(data.selection||null)});document.querySelector('.geo-btn').addEventListener('click',()=>{if(!navigator.geolocation)return;navigator.geolocation.getCurrentPosition(position=>{const point=[position.coords.latitude,position.coords.longitude];map.flyTo(point,13,{duration:.7});L.circle(point,{radius:Math.max(180,position.coords.accuracy),color:'#18a7cc',fillColor:'#18a7cc',fillOpacity:.08,weight:2}).addTo(map);},()=>{}, {enableHighAccuracy:false,timeout:8000,maximumAge:60000});});</script></body></html>`;
  }, [events, moments, theme.isLight, theme.pageBackground, theme.pageText, theme.pageTextMuted, theme.shellBackground, theme.surface]);

  return <View style={[styles.wrap, { backgroundColor: theme.pageBackground }]}>{createElement("iframe", {
    ref: (node: HTMLIFrameElement | null) => { iframeRef.current = node; },
    title: "Carte de découverte Neptune",
    srcDoc: html,
    onLoad: () => iframeRef.current?.contentWindow?.postMessage({ source: "connexio-discovery-parent", type: "selection", selection: selectedEntity ?? null }, "*"),
    style: { width: "100%", height: "100%", border: 0, display: "block", background: theme.pageBackground },
    allow: "geolocation"
  })}</View>;
}

const styles = StyleSheet.create({ wrap: { flex: 1, minHeight: 420, overflow: "hidden", borderRadius: 22 } });